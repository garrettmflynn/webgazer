import * as webgazer from './device.utils'

export const device =  {
    label: 'webgazer', 
    device: webgazer.device,
    onconnect: webgazer.onconnect,
    protocols: [],
}

export default device